I"Q(function() {
  jQuery(function() {
    var asignacions, bonificacions, haberes, personals;
    personals = $('#liquidacion_personal_id').html();
    $('#_subcategorium_id').change(function() {
      var options, subcategoria;
      subcategoria = $('#_subcategorium_id :selected').text();
      options = $(personals).filter("optgroup[label='" + subcategoria + "']").html();
      if (options) {
        $('#liquidacion_personal_id').parent().show();
        return $('#liquidacion_personal_id').html(options);
      } else {
        return $('#liquidacion_personal_id').empty();
      }
    });
    haberes = $('#liquidacion_habere_id').html();
    $('#_subcategorium_id').change(function() {
      var asignacion, options2;
      asignacion = $('#_subcategorium_id :selected').text();
      options2 = $(haberes).filter("optgroup[label='" + asignacion + "']").html();
      if (options2) {
        $('#liquidacion_habere_id').parent().show();
        return $('#liquidacion_habere_id').html(options2);
      } else {
        return $('#liquidacion_habere_id').empty();
      }
    });
    asignacions = $('#liquidacion_asignacion_id').html();
    $('#_subcategorium_id').change(function() {
      var asignacion, options3;
      asignacion = $('#_subcategorium_id :selected').text();
      options3 = $(asignacions).filter("optgroup[label='" + asignacion + "']").html();
      if (options3) {
        $('#liquidacion_asignacion_id').parent().show();
        return $('#liquidacion_asignacion_id').html(options3);
      } else {
        return $('#liquidacion_asignacion_id').empty();
      }
    });
    bonificacions = $('#liquidacion_bonificacion_id').html();
    $('#_subcategorium_id').change(function() {
      var asignacion, options4;
      asignacion = $('#_subcategorium_id :selected').text();
      options4 = $(bonificacions).filter("optgroup[label='" + asignacion + "']").html();
      if (options4) {
        $('#liquidacion_bonificacion_id').parent().show();
        return $('#liquidacion_bonificacion_id').html(options4);
      } else {
        return $('#liquidacion_bonificacion_id').empty();
      }
    });
    $('#_subcategorium_id').change(function() {
      var asignacion, bono, sueldo, total;
      sueldo = parseInt($('#liquidacion_habere_id option:selected').text());
      asignacion = parseInt($('#liquidacion_asignacion_id option:selected').text());
      bono = parseInt($('#liquidacion_bonificacion_id option:selected').text());
      if (isNaN(asignacion)) {
        total = sueldo + bono;
        return $('#total_haberes').val(total);
      } else {
        total = sueldo + asignacion + bono;
        return $('#total_haberes').val(total);
      }
    });
    return $('#liquidacion_bonificacion_id').change(function() {
      var asignacion, bono, sueldo, total;
      sueldo = parseInt($('#liquidacion_habere_id option:selected').text());
      asignacion = parseInt($('#liquidacion_asignacion_id option:selected').text());
      bono = parseInt($('#liquidacion_bonificacion_id option:selected').text());
      if (isNaN(bono)) {
        total = sueldo + asignacion;
        $('#total_haberes').val(total);
      }
      if (isNaN(asignacion)) {
        total = sueldo + bono;
        return $('#total_haberes').val(total);
      } else {
        total = sueldo + bono + asignacion;
        return $('#total_haberes').val(total);
      }
    });
  });

}).call(this);
:ET