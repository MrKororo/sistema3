<%= form_for(@descuento_adicional) do |f| %>
  <% if @descuento_adicional.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@descuento_adicional.errors.count, "error") %> prohibited this descuento_adicional from being saved:</h2>

      <ul>
      <% @descuento_adicional.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descuento_adicional %><br>
    <%= f.number_field :descuento_adicional %>
  </div>
  <div class="field">
    <%= f.label :detalle %><br>
    <%= f.text_field :detalle %>
  </div>
  <div class="actions">
    <%= f.submit ( f.object.new_record? ? "Crear" : "Actualizar"), class: "btn" %>
    <%= f.submit "Limpiar", :type => "reset" %>
  </div>

<% end %>
